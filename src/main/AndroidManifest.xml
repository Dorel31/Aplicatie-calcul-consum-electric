<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pa8">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PA8">
        <activity
            android:name=".Consumption"
            android:exported="false"
            android:label="Consum"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:parentActivityName=".Dispozitive" />
        <activity
            android:name=".AddActivity"
            android:exported="false"
            android:label="Adăugare dispozitiv"
            android:parentActivityName=".Dispozitive" />
        <activity
            android:name=".Dispozitive"
            android:exported="false"
            android:label="Dispozitive"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>